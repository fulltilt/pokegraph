apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: pokemon-app
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: pokemon-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: frontend
        image: ghcr.io/fulltilt/pokemon-frontend:latest
        ports:
        - containerPort: 80
        envFrom:
        - secretRef:
            name: backend-secrets