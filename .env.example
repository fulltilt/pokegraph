# PostgreSQL settings used by docker-compose.yml
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=pokedex

# Backend connection string used by docker-compose.yml
# Keep host as 'db' when running inside Docker Compose network.
DATABASE_URL=postgres://postgres:password@db:5432/pokedex
